services:
  - type: web
    name: chatfinal
    env: java
    buildCommand: mvn clean install
    startCommand: java -jar target/*.jar
    envVars:
      - key: OPENAI_API_KEY
        value: ${OPENAI_API_KEY}
